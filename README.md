# Quick start
1. Run [docker-compose](./docker-compose.yml)
2. Run application. For example, via gradle
    ```shell script
    ./gradlew clean bootRun
    ```
3. Go to http://localhost:8080/swagger/ui
4. Select definition - "admin"